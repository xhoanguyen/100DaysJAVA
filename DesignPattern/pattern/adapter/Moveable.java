package pattern.adapter;

public interface Moveable {

    // the original interface Movable which is supposed to return the speed of some
    // luxury cars in miles per hour

    double getSpeed();

}
